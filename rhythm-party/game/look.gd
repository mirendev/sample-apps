class_name Look
extends RefCounted

## Shared palette. Borrowed from rust-chat's accent so the Miren family
## resemblance shows. Referenced as Look.ACCENT etc. (no instance needed).

const ACCENT := Color("#F6834B")
const BG := Color("#16161C")
const DIM := Color("#3A3A44")
const NOTE := Color("#EDEDF2")
